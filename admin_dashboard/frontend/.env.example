FRONTEND_PORT=5183
# Must match backend/.env's BACKEND_PORT -- used as the /api and /ws proxy target. Defaults to
# 8001, not 8000, because the game's own backend is hardcoded to port 8000 and this admin
# backend needs to run alongside it locally without clashing.
BACKEND_PORT=8001
