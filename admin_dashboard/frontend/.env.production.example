# Production environment template for the ADMIN DASHBOARD frontend.
# Copy this to .env and fill in the placeholder below.

FRONTEND_PORT=REPLACE_WITH_PRODUCTION_PORT
# Must match admin_dashboard/backend's production BACKEND_PORT
# (see admin_dashboard/backend/.env.production.example).
BACKEND_PORT=REPLACE_WITH_PRODUCTION_PORT
